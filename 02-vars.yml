- name: Demo on Variable
  hosts:
    - all
  vars:
    URL1: playgoogle.com
  vars_files:
  - vars.yml
  tasks:
  - name: Print Variable from Play
    debug:
     msg: URL = {{URL1}}
  - name: Print Variable from File
    debug:
     msg: URL = {{URL2}}